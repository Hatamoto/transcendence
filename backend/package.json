{
    "name": "backend-root",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm run devAuth --prefix authentication_server\" \"npm run dev --prefix server\""
    },
    "devDependencies": {
        "concurrency": "^0.1.4",
        "concurrently": "^9.1.2"
    }
}
